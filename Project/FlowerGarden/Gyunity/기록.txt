


/// 2023.07.29
// 마우스로 박스를 선택한 다음 조금이라도 움직이면 박스가 움직이는
// 것을 막음. -> 여유를 두었음. 조금이라도 마우스를 움직이면 박스는 안 움직이지만
// 내가 설정한 일정 범위를 넘으면 마우스로 상자가 따라 옴.

// SpriteInfoView로 현재 선택한 상자의 정보를 바꿀 수 있게 만드는 중

// pivot의 위치를 좌표로 하는 것이 아닌 좌상단 기준으로 바꾸어야 할 듯.

// SpriteInfoView(좌하단) 로 sprite의 정보 변경 가능


/// 2023.07.30 
// 00:05 : 만들어야 할 것 기록
// 1. 선택된 애니메이션을 queue에 추가.
// 2. queue를 가지고 있는 목록 추가.
// 3. 목록에서 선택된 애니메이션 queue를 재생시키는 view를 만들 것.
// 4. ..?
// 5. 高...
// 6. 뽑아내는 방식이 sprite일 경우 : (name, x, y, cx, cy, px, py)
// 7. 뽑아내는 방식이 spriteAnimation일 경우
// queue에 저장한 sprite들을 내보내야 함.

/// 00:54
//	SpriteAnimation 추가
//	1. shift or ctrl키로 다중 선택 후 현재 선택 된 extractedSprite들을 queue에 저장하는 방법
//	-> 이 경우 번호를 설정해야 함
//	-> 동일한 이름도 자동 설정해주어야 할 듯 ex) idle_0, idle_1
//	-> 졸라 귀찮음.
//	이 경우 SpriteAnimation은 무엇을 가져야 하는가??
//	-> ExtractedSprite*를 vector로 가지고 있는 편이 좋아보인다.
//		-> 순서대로 가지고 있어야 함.

// mainDoc에서는 SpriteAnimation들을 unordered_map으로 가지고 있으면 될 듯?

// 01:28 GyunityView에 m_seletedBoxQueue만들기까지 함. 
// pivot 계산 방식 수정, slice버튼을 누르면 자동으로 생기는 상자들의 이름을 파일의 이름 + index로 만드는 방식 추가

// 13:30 다중 선택 시작 -> 완료
//
// 15:36 다중 선택한 스프라이트들을 애니메이션으로 만드는 것을 해야 함.
// 1. 선택한 순서 팝업으로 띄우기 (번호 렌더)
// 2. 애니메이션 리스트 만들기
// 3. 애니메이션을 재생할 view 만들기
// 4.


// 20:44 일단 파일 내보내기 만들기
// 1. 스프라이트 txt 내보내기
// -> 파일 경로 및 이름
// -> 스프라이트 수
// -> 스프라이트의 수 만큼 각자의 정보 입력


// DataControl 임시 데이터로 저장하고 있어서 수정해야 함.
// file저장은 되지만 외계어로 저장 중. 수정해야 함.


//// 2023.08.01 
// 01:01 애니메이션 리스트 save 수정작업 시작